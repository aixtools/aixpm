# Install variables for unzip
# pkg_creates is tested. If it exists, no further steps are taken
# {{ packages }} contains one or more files to download.
# {{ src_t(ype) }} will be used later to modify the 'fetch' behavior
---
# Install variables for unzip
pkg_creates: "/opt/bin/unzip"
packages:
  - package: 'unzip'
    lpp: "aixtools.unzip"
    vrmf: "6.0.0.0"
    lppg: "all"
    lppdoc: ""
    src: "download.aixtools.net/tools/"
    src_t: "http"
    chksum: "md5:9CD3E8942776B2235BC442AF4B2AC1B8"
